# Base nginx config for reverse proxy / static hosting. Extend per environment.
# Example: serve frontend from /app/dist and proxy /api to backend services.

worker_processes auto;
error_log /var/log/nginx/error.log warn;
pid /var/run/nginx.pid;

events {
    worker_connections 1024;
}

http {
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;

    log_format  main  '$remote_addr - $remote_user [$time_local] "$request" '
                      '$status $body_bytes_sent "$http_referer" '
                      '"$http_user_agent" "$http_x_forwarded_for"';

    access_log  /var/log/nginx/access.log  main;

    sendfile        on;
    keepalive_timeout  65;

    server {
        listen 80;
        server_name _;

        # Example static hosting (adjust root to your build output)
        # root /usr/share/nginx/html;
        # try_files $uri $uri/ /index.html;

        # Example API proxy (adjust upstreams per service)
        # location /api/ {
        #     proxy_pass http://node-api:3001/;
        #     proxy_set_header Host $host;
        #     proxy_set_header X-Real-IP $remote_addr;
        #     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        # }
    }
}

//! Assembly stage: validation, DOF mapping, global stiffness/force build, springs, self-weight.
//! This module is intentionally slim; heavy helpers live in `loads` and `elements`.

// TODO: Move existing assembly logic from `mod.rs` here.

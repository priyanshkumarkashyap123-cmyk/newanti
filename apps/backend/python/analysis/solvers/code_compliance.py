"""\ncode_compliance.py - Check results against Indian Standards limits\n\nVerifies:\n- IS 456:2000 (Reinforced Concrete) - stress, deflection limits\n- IS 800:2007 (Steel) - stress ratios, slenderness\n- IS 875:1987 (Wind loads) - minimum wind speeds\n\"\"\"\n\nimport numpy as np\nfrom numpy import float64, ndarray\nfrom typing import Dict, Tuple, Optional\nfrom dataclasses import dataclass, asdict\nimport logging\n\nlogger = logging.getLogger(__name__)\n\n\n@dataclass\nclass ComplianceCheck:\n    \"\"\"Result of a single compliance check.\"\"\"\n    check_name: str                # e.g., 'concrete_stress'\n    pass_fail: bool                # True if passes\n    value: float64                 # Actual value\n    limit: float64                 # Code limit\n    design_ratio: float64          # value / limit (should be ≤ 1.0)\n    code_reference: str            # e.g., 'IS 456:2000 Cl. 40.1'\n    severity: str                  # 'error', 'warning', 'info'\n    message: str                   # Human readable\n\n\nclass IS456Checker:\n    \"\"\"\n    IS 456:2000 - Reinforced Concrete Code compliance checks.\n    \"\"\"\n\n    # Material properties (default)\n    CONCRETE_GRADES = {\n        'M20': {'fck': 20.0, 'modulus': 30000},\n        'M25': {'fck': 25.0, 'modulus': 32000},\n        'M30': {'fck': 30.0, 'modulus': 34000},\n        'M35': {'fck': 35.0, 'modulus': 36000},\n        'M40': {'fck': 40.0, 'modulus': 37000},\n    }\n\n    REBAR_GRADES = {\n        'Fe415': {'fy': 415.0},\n        'Fe500': {'fy': 500.0},\n        'Fe500S': {'fy': 500.0},\n    }\n\n    @staticmethod\n    def check_concrete_stress(stress_mpa: float64,\n                            concrete_grade: str = 'M30') -> ComplianceCheck:\n        \"\"\"\n        Check concrete compressive stress.\n\n        IS 456:2000 Clause 39.1 - Permissible stress = 0.33*fck\n\n        Parameters\n        ----------\n        stress_mpa : float\n            Actual stress (MPa)\n        concrete_grade : str\n            Grade of concrete (M20, M25, M30, M35, M40)\n\n        Returns\n        -------\n        check : ComplianceCheck\n        \"\"\"\n        if concrete_grade not in IS456Checker.CONCRETE_GRADES:\n            raise ValueError(f\"Unknown grade: {concrete_grade}\")\n\n        fck = float64(IS456Checker.CONCRETE_GRADES[concrete_grade]['fck'])\n        permissible = float64(0.33 * fck)  # MPa\n\n        passes = abs(stress_mpa) <= permissible\n        design_ratio = float64(abs(stress_mpa) / permissible) if permissible > 0 else float64(999)\n\n        return ComplianceCheck(\n            check_name='concrete_compressive_stress',\n            pass_fail=passes,\n            value=float64(stress_mpa),\n            limit=permissible,\n            design_ratio=design_ratio,\n            code_reference='IS 456:2000 Clause 39.1',\n            severity='error' if not passes else 'info',\n            message=(\n                f\"Concrete stress {stress_mpa:.2f} MPa vs \"\n                f\"permissible {permissible:.2f} MPa (0.33*fck for {concrete_grade})\"\n            )\n        )\n\n    @staticmethod\n    def check_deflection(deflection_mm: float64,\n                        span_mm: float64,\n                        element_type: str = 'beam') -> ComplianceCheck:\n        \"\"\"\n        Check member deflection limits.\n\n        IS 456:2000 Table 4 - Deflection limits are span/180 to span/240\n        - Beams: span/240 (sensitive)\n        - Columns: span/150 (less critical)\n\n        Parameters\n        ----------\n        deflection_mm : float\n            Maximum deflection (mm)\n        span_mm : float\n            Member span (mm)\n        element_type : str\n            'beam', 'column', or 'cantilever'\n\n        Returns\n        -------\n        check : ComplianceCheck\n        \"\"\"\n        # Deflection limits\n        limits = {\n            'beam': span_mm / 240,          # Most stringent\n            'cantilever': span_mm / 180,    # Easier\n            'column': span_mm / 150,        # For buckling, not deflection\n        }\n\n        if element_type not in limits:\n            element_type = 'beam'  # Default\n\n        limit = float64(limits[element_type])\n        actual = float64(abs(deflection_mm))\n        passes = actual <= limit\n        design_ratio = float64(actual / limit) if limit > 0 else float64(999)\n\n        return ComplianceCheck(\n            check_name='deflection_limit',\n            pass_fail=passes,\n            value=actual,\n            limit=limit,\n            design_ratio=design_ratio,\n            code_reference='IS 456:2000 Table 4',\n            severity='warning' if not passes else 'info',\n            message=(\n                f\"{element_type.title()} deflection {actual:.2f} mm vs \"\n                f\"limit {limit:.2f} mm (span/{span_mm/limit:.0f})\"\n            )\n        )\n\n\nclass IS800Checker:\n    \"\"\"\n    IS 800:2007 - Steel Code compliance checks.\n    \"\"\"\n\n    STEEL_GRADES = {\n        'Fe250': {'fy': 250, 'fu': 410},\n        'Fe410': {'fy': 410, 'fu': 540},\n        'Fe450': {'fy': 450, 'fu': 540},\n        'Fe550': {'fy': 550, 'fu':620},\n    }\n\n    @staticmethod\n    def check_steel_stress(stress_mpa: float64,\n                          steel_grade: str = 'Fe410') -> ComplianceCheck:\n        \"\"\"\n        Check steel tensile/compressive stress.\n\n        IS 800:2007 Clause 5.7.1\n        Permissible stress = 0.6*fy for tension/compression\n\n        Parameters\n        ----------\n        stress_mpa : float\n            Actual stress (MPa)\n        steel_grade : str\n            Grade of steel (Fe250, Fe410, Fe450, Fe550)\n\n        Returns\n        -------\n        check : ComplianceCheck\n        \"\"\"\n        if steel_grade not in IS800Checker.STEEL_GRADES:\n            raise ValueError(f\"Unknown grade: {steel_grade}\")\n\n        fy = float64(IS800Checker.STEEL_GRADES[steel_grade]['fy'])\n        permissible = float64(0.6 * fy)  # MPa\n\n        passes = abs(stress_mpa) <= permissible\n        design_ratio = float64(abs(stress_mpa) / permissible) if permissible > 0 else float64(999)\n\n        return ComplianceCheck(\n            check_name='steel_stress',\n            pass_fail=passes,\n            value=float64(stress_mpa),\n            limit=permissible,\n            design_ratio=design_ratio,\n            code_reference='IS 800:2007 Clause 5.7.1',\n            severity='error' if not passes else 'info',\n            message=(\n                f\"Steel stress {stress_mpa:.2f} MPa vs \"\n                f\"permissible {permissible:.2f} MPa (0.6*fy for {steel_grade})\"\n            )\n        )\n\n    @staticmethod\n    def check_slenderness(length_mm: float64,\n                         radius_gyration_mm: float64,\n                         member_type: str = 'column') -> ComplianceCheck:\n        \"\"\"\n        Check slenderness ratio.\n\n        IS 800:2007 Clause 5.1.1 - Limits λ = L/r\n        - Columns: λ ≤ 200\n        - Tension members: λ ≤ 250\n        - Angles: λ ≤ 180\n\n        Parameters\n        ----------\n        length_mm : float\n            Effective length (mm)\n        radius_gyration_mm : float\n            Radius of gyration (mm)\n        member_type : str\n            'column', 'tension', 'angle'\n\n        Returns\n        -------\n        check : ComplianceCheck\n        \"\"\"\n        limits = {\n            'column': 200,\n            'tension': 250,\n            'angle': 180,\n        }\n\n        if member_type not in limits:\n            member_type = 'column'\n\n        limit = float64(limits[member_type])\n        actual = float64(length_mm / radius_gyration_mm)\n        passes = actual <= limit\n        design_ratio = float64(actual / limit) if limit > 0 else float64(999)\n\n        return ComplianceCheck(\n            check_name='slenderness_ratio',\n            pass_fail=passes,\n            value=actual,\n            limit=limit,\n            design_ratio=design_ratio,\n            code_reference='IS 800:2007 Clause 5.1.1',\n            severity='warning' if not passes else 'info',\n            message=(\n                f\"{member_type.title()} slenderness λ = {actual:.1f} vs \"\n                f\"limit {limit:.1f}\"\n            )\n        )\n\n    @staticmethod\n    def check_shear_buckling(height_mm: float64,\n                            thickness_mm: float64) -> ComplianceCheck:\n        \"\"\"\n        Check web shear buckling (plate girder webs).\n\n        IS 800:2007 Table 4 - d/t ≤ 85*sqrt(250/fy) for Fe410\n\n        Parameters\n        ----------\n        height_mm : float\n            Web height (mm)\n        thickness_mm : float\n            Web thickness (mm)\n\n        Returns\n        -------\n        check : ComplianceCheck\n        \"\"\"\n        fy = float64(410)  # Default Fe410\n        limit = float64(85 * np.sqrt(250 / fy))\n        actual = float64(height_mm / thickness_mm)\n        passes = actual <= limit\n        design_ratio = float64(actual / limit) if limit > 0 else float64(999)\n\n        return ComplianceCheck(\n            check_name='web_shear_buckling',\n            pass_fail=passes,\n            value=actual,\n            limit=limit,\n            design_ratio=design_ratio,\n            code_reference='IS 800:2007 Table 4',\n            severity='warning' if not passes else 'info',\n            message=(\n                f\"Web d/t = {actual:.1f} vs limit {limit:.1f}\"\n            )\n        )\n\n\nclass CodeCompliance:\n    \"\"\"\n    Master compliance checker.\n\n    Aggregates checks across all codes.\n    \"\"\"\n\n    @staticmethod\n    def run_checks(results: Dict,\n                   code_type: str = 'is456',\n                   material_grade: str = 'M30') -> Dict[str, list]:\n        \"\"\"\n        Run all applicable compliance checks.\n\n        Parameters\n        ----------\n        results : Dict\n            Analysis results with stresses, deflections, etc.\n        code_type : str\n            'is456' (concrete) or 'is800' (steel)\n        material_grade : str\n            Material grade for code limits\n\n        Returns\n        -------\n        checks : Dict[str, list]\n            Sorted by status: {'passed': [...], 'failed': [...], 'warnings': [...]}\n        \"\"\"\n        all_checks = []\n\n        if code_type.lower() == 'is456':\n            # Example checks (expand based on results)\n            if 'max_stress' in results:\n                check = IS456Checker.check_concrete_stress(\n                    float64(results['max_stress']),\n                    material_grade\n                )\n                all_checks.append(check)\n\n            if 'max_deflection' in results:\n                check = IS456Checker.check_deflection(\n                    float64(results['max_deflection']),\n                    float64(results.get('span', 5000)),\n                    'beam'\n                )\n                all_checks.append(check)\n\n        elif code_type.lower() == 'is800':\n            if 'max_stress' in results:\n                check = IS800Checker.check_steel_stress(\n                    float64(results['max_stress']),\n                    material_grade\n                )\n                all_checks.append(check)\n\n        # Organize results\n        checks_dict = {\n            'passed': [],\n            'failed': [],\n            'warnings': [],\n            'info': [],\n        }\n\n        for check in all_checks:\n            check_data = asdict(check)\n\n            if not check.pass_fail:\n                checks_dict['failed'].append(check_data)\n            elif check.severity == 'warning':\n                checks_dict['warnings'].append(check_data)\n            elif check.severity == 'info':\n                checks_dict['info'].append(check_data)\n            else:\n                checks_dict['passed'].append(check_data)\n\n        logger.info(\n            f\"Compliance checks: {len(checks_dict['passed'])} passed, \"\n            f\"{len(checks_dict['failed'])} failed, \"\n            f\"{len(checks_dict['warnings'])} warnings\"\n        )\n\n        return checks_dict\n\n    @staticmethod\n    def summary(checks: Dict[str, list]) -> str:\n        \"\"\"\n        Generate human-readable compliance summary.\n        \"\"\"\n        lines = [\n            \"\\n=== CODE COMPLIANCE SUMMARY ===\",\n            f\"✓ Passed:   {len(checks['passed'])}\",\n            f\"⚠ Warnings: {len(checks['warnings'])}\",\n            f\"✗ Failed:   {len(checks['failed'])}\",\n        ]\n\n        if checks['failed']:\n            lines.append(\"\\nFailed Checks:\")\n            for check in checks['failed']:\n                lines.append(\n                    f\"  {check['check_name']}: {check['message']} \"\n                    f\"(DR={check['design_ratio']:.2f})\"\n                )\n\n        if checks['warnings']:\n            lines.append(\"\\nWarnings:\")\n            for check in checks['warnings']:\n                lines.append(\n                    f\"  {check['check_name']}: {check['message']} \"\n                    f\"(DR={check['design_ratio']:.2f})\"\n                )\n\n        return \"\\n\".join(lines)\n\n\nif __name__ == \"__main__\":\n    logging.basicConfig(level=logging.INFO)\n\n    # Test concrete check\n    check1 = IS456Checker.check_concrete_stress(float64(8.5), 'M30')\n    print(f\"Stress check pass: {check1.pass_fail}\")\n    print(f\"Design ratio: {check1.design_ratio:.2f}\")\n\n    # Test deflection\n    check2 = IS456Checker.check_deflection(float64(15), float64(5000), 'beam')\n    print(f\"\\nDeflection check pass: {check2.pass_fail}\")\n    print(f\"Design ratio: {check2.design_ratio:.2f}\")\n
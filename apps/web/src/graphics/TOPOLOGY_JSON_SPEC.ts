/**\n * TOPOLOGY JSON EXPORT SPECIFICATION\n * \n * Complete specification of the JSON format exported by CanvasManager.exportTopologyJSON()\n * This is what gets sent to the backend for structural analysis.\n */\n\n/**\n * StructuralTopology Interface\n * \n * {\n *   \"nodes\": Array<Node>,\n *   \"elements\": Array<Element>\n * }\n * \n * NOTES:\n * - All coordinates are in meters (m)\n * - All node IDs and element IDs are globally unique strings\n * - IDs are automatically assigned by client (timestamp-based)\n * - This is a strictly deterministic export (same model → same JSON)\n */\n\ninterface Node {\n  id: string;      // Unique identifier (e.g., \"N1234567890\")\n  x: number;       // Global X coordinate (meters)\n  y: number;       // Global Y coordinate (meters, positive = up)\n  z: number;       // Global Z coordinate (meters)\n}\n\ninterface Element {\n  id: string;                  // Unique identifier (e.g., \"M1234567890\")\n  startNodeId: string;         // References nodes[].id\n  endNodeId: string;           // References nodes[].id\n  // Note: Member type is NOT included in topology export\n  //       Backend infers from member section data (separate API)\n}\n\ninterface StructuralTopology {\n  nodes: Node[];\n  elements: Element[];\n}\n\n// ============================================================================\n// COMPLETE EXAMPLE: TWO-SPAN BEAM\n// ============================================================================\n\nconst EXAMPLE_TOPOLOGY: StructuralTopology = {\n  nodes: [\n    { id: \"N1\", x: 0.0, y: 0.0, z: 0.0 },   // Left support\n    { id: \"N2\", x: 5.0, y: 0.0, z: 0.0 },   // Middle support (span 1)\n    { id: \"N3\", x: 10.0, y: 0.0, z: 0.0 },  // Right support (span 2)\n  ],\n  elements: [\n    { id: \"M1\", startNodeId: \"N1\", endNodeId: \"N2\" },   // First span\n    { id: \"M2\", startNodeId: \"N2\", endNodeId: \"N3\" },   // Second span\n  ],\n};\n\n// ============================================================================\n// BACKEND API CONTRACT\n// ============================================================================\n\n/**\n * POST /api/v1/analysis/structural\n * \n * Request Body:\n * {\n *   \"nodes\": [ ... ],\n *   \"elements\": [ ... ],\n *   \"loads\": [ ... ],              // Optional: NodeLoad[]\n *   \"memberLoads\": [ ... ],        // Optional: MemberLoad[]\n *   \"loadCases\": [ ... ],          // Optional: LoadCase[]\n *   \"code\": \"IS 456:2000\",         // Design code (default: IS 456:2000)\n *   \"options\": {                   // Optional: Analysis options\n *     \"linearAnalysis\": true,      // Linear vs nonlinear\n *     \"selfWeight\": true,          // Include self-weight\n *     \"damping\": 0.05              // Damping ratio for dynamics\n *   }\n * }\n * \n * Response Body:\n * {\n *   \"status\": \"completed\" | \"failed\" | \"timeout\",\n *   \"displacements\": {\n *     \"N1\": { \"dx\": 0.0, \"dy\": -0.025, \"dz\": 0.0, \"rx\": 0.001, \"ry\": 0, \"rz\": 0 },\n *     \"N2\": { \"dx\": 0.0, \"dy\": -0.050, \"dz\": 0.0, \"rx\": 0.000, \"ry\": 0, \"rz\": 0 },\n *     ...\n *   },\n *   \"reactions\": {\n *     \"N1\": { \"fx\": 0.0, \"fy\": 75.0, \"fz\": 0.0, \"mx\": 0.0, \"my\": 0.0, \"mz\": 0.0 },\n *     \"N3\": { \"fx\": 0.0, \"fy\": 75.0, \"fz\": 0.0, \"mx\": 0.0, \"my\": 0.0, \"mz\": 0.0 },\n *     ...\n *   },\n *   \"memberForces\": {\n *     \"M1\": {\n *       \"axial\": 0.0,\n *       \"shearY\": 50.0,\n *       \"shearZ\": 0.0,\n *       \"momentY\": 0.0,\n *       \"momentZ\": -62.5,     // Maximum negative moment at center span\n *       \"torsion\": 0.0,\n *       \"diagramData\": {\n *         \"x_values\": [0, 1, 2, 3, 4, 5],     // Positions along member\n *         \"shear_y\": [50, 50, 50, 50, 50, 50],\n *         \"moment_z\": [0, -12.5, -25, -37.5, -50, -62.5],\n *         \"axial\": [0, 0, 0, 0, 0, 0]\n *       }\n *     },\n *     ...\n *   },\n *   \"stats\": {\n *     \"solveTimeMs\": 125,\n *     \"assemblyTimeMs\": 45,\n *     \"totalTimeMs\": 170,\n *     \"method\": \"Direct Stiffness Method (DSM)\",\n *     \"usedCloud\": false,\n *     \"conditionNumber\": 1e6\n *   }\n * }\n */\n\n// ============================================================================\n// INTEGRATION EXAMPLE: SENDING TOPOLOGY TO BACKEND\n// ============================================================================\n\nasync function runAnalysis(topology: StructuralTopology) {\n  try {\n    const response = await fetch('/api/v1/analysis/structural', {\n      method: 'POST',\n      headers: {\n        'Content-Type': 'application/json',\n        'Authorization': `Bearer ${getAuthToken()}`,\n      },\n      body: JSON.stringify({\n        // Core topology (required)\n        nodes: topology.nodes,\n        elements: topology.elements,\n\n        // Load cases (optional but recommended)\n        loads: [\n          // Node loads: concentrated forces\n          {\n            id: 'P1',\n            nodeId: 'N2',\n            fx: 0,\n            fy: -50, // 50 kN downward\n            fz: 0,\n            mx: 0,\n            my: 0,\n            mz: 0,\n          },\n        ],\n        memberLoads: [\n          // Member loads: distributed loads (UDL, UVL, point loads)\n          {\n            id: 'UDL1',\n            memberId: 'M1',\n            type: 'UDL',\n            w1: 10, // 10 kN/m\n            w2: 10,\n            direction: 'global_y', // Downward\n            startPos: 0, // Start at left node\n            endPos: 1, // End at right node (full length)\n          },\n        ],\n        loadCases: [\n          {\n            id: 'DL',\n            name: 'Dead Load',\n            type: 'dead',\n            loads: [],\n            memberLoads: [\n              {\n                id: 'DL1',\n                memberId: 'M1',\n                type: 'UDL',\n                w1: 5,\n                w2: 5,\n                direction: 'global_y',\n              },\n            ],\n            selfWeight: true,\n          },\n          {\n            id: 'LL',\n            name: 'Live Load',\n            type: 'live',\n            loads: [],\n            memberLoads: [\n              {\n                id: 'LL1',\n                memberId: 'M1',\n                type: 'UDL',\n                w1: 10,\n                w2: 10,\n                direction: 'global_y',\n              },\n            ],\n          },\n        ],\n        loadCombinations: [\n          {\n            id: 'ULS1',\n            name: 'Ultimate Limit State (IS 875)',\n            code: 'IS 875',\n            factors: [\n              { loadCaseId: 'DL', factor: 1.5 },\n              { loadCaseId: 'LL', factor: 1.5 },\n            ],\n          },\n        ],\n\n        // Design code and options\n        code: 'IS 456:2000', // IS 456 for concrete, IS 800 for steel\n        options: {\n          linearAnalysis: true,\n          considSelfWeight: true,\n          materialType: 'steel', // 'steel', 'concrete', 'timber'\n        },\n      }),\n    });\n\n    if (!response.ok) {\n      throw new Error(`Analysis failed: ${response.statusText}`);\n    }\n\n    const results = await response.json();\n    console.log('Analysis complete:', results);\n    return results;\n  } catch (error) {\n    console.error('Analysis error:', error);\n    throw error;\n  }\n}\n\n// ============================================================================\n// VALIDATION RULES FOR TOPOLOGY\n// ============================================================================\n\n/**\n * Backend should validate:\n * \n * 1. NODES\n *    - Id is non-empty string\n *    - x, y, z are valid numbers (not NaN, not Infinity)\n *    - Coordinates are within reasonable bounds: -1000m to +10000m\n *    - No duplicate node IDs\n * \n * 2. ELEMENTS\n *    - Id is non-empty string\n *    - startNodeId exists in nodes array\n *    - endNodeId exists in nodes array\n *    - startNodeId ≠ endNodeId (no self-loops)\n *    - No duplicate element IDs\n *    - Member length > 0.1m (minimum element length)\n * \n * 3. LOADS\n *    - nodeId/memberId reference valid nodes/elements\n *    - Force/moment magnitudes are reasonable (< 1000 MN)\n * \n * 4. OVERALL\n *    - At least 2 nodes\n *    - At least 1 element\n *    - Model has at least some restraints (otherwise solution is undefined)\n */\n\n// ============================================================================\n// PERFORMANCE: EXPORT FREQUENCY\n// ============================================================================\n\n/**\n * The canvas exports topology every 60 frames:\n * \n * 60 FPS → 1 export per second\n * \n * For real-time analysis:\n * - Don't send every export (too many requests)\n * - Buffer/debounce: Send only if topology changed\n * - Throttle: Send at most once per 5 seconds\n * \n * Example throttle:\n * \n * let lastSentTopo = \"\";\n * let nextSendTime = 0;\n * \n * function handleTopologyExport(json: string) {\n *   if (json === lastSentTopo) return; // No change\n *   if (Date.now() < nextSendTime) return; // Throttled\n *   \n *   lastSentTopo = json;\n *   nextSendTime = Date.now() + 5000; // 5 second throttle\n *   \n *   runAnalysis(JSON.parse(json));\n * }\n */\n\nexport const TOPOLOGY_SPEC = {\n  specification: 'StructuralTopology',\n  version: '1.0.0',\n  coordinates: 'Global Cartesian (meters)',\n  example: EXAMPLE_TOPOLOGY,\n};\n
"""\nmember_forces.py - Calculate member internal forces from nodal displacements\n\nBack-calculates element internal forces (axial, shear, moment)  from global\nnode displacements using local element stiffness matrices.\n\nOutput: Member forces at start/end nodes for each element\n\"\"\"\n\nimport numpy as np\nfrom numpy import float64, ndarray\nfrom typing import Dict, Tuple, Optional\nimport logging\n\nfrom dsm_3d_frame import (\n    Element3D, Node3D, StructuralModel,\n    TimoshenkoBeamElement3D,\n    CoordinateTransformations3D\n)\n\nlogger = logging.getLogger(__name__)\n\n\nclass MemberForceCalculator:\n    \"\"\"\n    Calculate internal forces and moments in members.\n    \n    Process:\n    1. Extract local nodal displacements for each element\n    2. Multiply by local stiffness matrix: {f} = [k]{u}\n    3. Convert forces/moments to standard notation\n    \"\"\"\n\n    @staticmethod\n    def calculate_member_forces(model: StructuralModel,\n                               displacements: ndarray) -> Dict[str, Dict[str, float64]]:\n        \"\"\"\n        Calculate internal forces for all members.\n\n        Parameters\n        ----------\n        model : StructuralModel\n            Structural model with nodes and elements\n        displacements : ndarray (n_dofs,)\n            Global nodal displacements [Ux, Uy, Uz, Rx, Ry, Rz, ...]\n\n        Returns\n        -------\n        member_forces : Dict[str, Dict]\n            Dictionary mapping member ID to force dictionary:\n            {\n              'M1': {\n                'axial_i': float,      # Axial force at node i (kN)\n                'shear_y_i': float,    # Shear in Y at node i (kN)\n                'shear_z_i': float,    # Shear in Z at node i (kN)\n                'moment_x_i': float,   # Torsion at node i (kN·m)\n                'moment_y_i': float,   # Moment about Y at node i (kN·m)\n                'moment_z_i': float,   # Moment about Z at node i (kN·m)\n                'axial_j': float,      # Forces at node j\n                'shear_y_j': float,\n                ...\n              },\n              ...\n            }\n        \"\"\"\n        member_forces = {}\n\n        # Node index mapping\n        node_list = sorted(model.nodes.keys())\n        node_to_index = {nid: idx for idx, nid in enumerate(node_list)}\n\n        # Process each element\n        for elem_id, element in model.elements.items():\n            node_i = model.nodes[element.node_i]\n            node_j = model.nodes[element.node_j]\n\n            # Member length\n            dx = node_j.x - node_i.x\n            dy = node_j.y - node_i.y\n            dz = node_j.z - node_i.z\n            L = np.sqrt(dx**2 + dy**2 + dz**2)\n\n            # ================================================================\n            # EXTRACT LOCAL DISPLACEMENTS\n            # ================================================================\n            i_idx = node_to_index[element.node_i]\n            j_idx = node_to_index[element.node_j]\n\n            # Global DOF indices\n            dofs_i = np.array([i_idx*6, i_idx*6+1, i_idx*6+2,\n                              i_idx*6+3, i_idx*6+4, i_idx*6+5], dtype=int)\n            dofs_j = np.array([j_idx*6, j_idx*6+1, j_idx*6+2,\n                              j_idx*6+3, j_idx*6+4, j_idx*6+5], dtype=int)\n\n            dofs = np.concatenate([dofs_i, dofs_j])\n\n            # Global displacements for this element\n            U_global = displacements[dofs].reshape(12, 1)\n\n            # ================================================================\n            # TRANSFORM TO LOCAL COORDINATES\n            # ================================================================\n            cos_mat = CoordinateTransformations3D.direction_cosines(node_i, node_j)\n            T = CoordinateTransformations3D.transformation_matrix_12x12(cos_mat)\n\n            # Local displacements: {u} = [T] {U}\n            U_local = T @ U_global\n            U_local = U_local.flatten()\n\n            # ================================================================\n            # COMPUTE LOCAL ELEMENT FORCES\n            # ================================================================\n            k_local = TimoshenkoBeamElement3D.local_stiffness_matrix(\n                element, float64(L)\n            )\n\n            # Local forces: {f} = [k]{u}\n            f_local = k_local @ U_local\n\n            # ================================================================\n            # EXTRACT FORCE COMPONENTS\n            # ================================================================\n            # DOF layout: [u_xi, u_yi, u_zi, θ_xi, θ_yi, θ_zi,\n            #              u_xj, u_yj, u_zj, θ_xj, θ_yj, θ_zj]\n            \n            # Forces at node i (start)‌\n            axial_i = float64(f_local[0])    # Along member\n            shear_y_i = float64(f_local[1])   # Perpendicular (y-direction)\n            shear_z_i = float64(f_local[2])   # Perpendicular (z-direction)\n            moment_x_i = float64(f_local[3])  # About member axis (torsion)\n            moment_y_i = float64(f_local[4])  # Bending moment (y)\n            moment_z_i = float64(f_local[5])  # Bending moment (z)\n\n            # Forces at node j (end)\n            axial_j = float64(f_local[6])\n            shear_y_j = float64(f_local[7])\n            shear_z_j = float64(f_local[8])\n            moment_x_j = float64(f_local[9])\n            moment_y_j = float64(f_local[10])\n            moment_z_j = float64(f_local[11])\n\n            # Store forces\n            member_forces[elem_id] = {\n                # Forces at node i\n                'axial_i': axial_i,\n                'shear_y_i': shear_y_i,\n                'shear_z_i': shear_z_i,\n                'moment_x_i': moment_x_i,  # Torsion\n                'moment_y_i': moment_y_i,   # My\n                'moment_z_i': moment_z_i,   # Mz\n\n                # Forces at node j\n                'axial_j': axial_j,\n                'shear_y_j': shear_y_j,\n                'shear_z_j': shear_z_j,\n                'moment_x_j': moment_x_j,\n                'moment_y_j': moment_y_j,\n                'moment_z_j': moment_z_j,\n\n                # Member properties for reference\n                'length': float64(L),\n                'element_type': 'beam_3d_timoshenko',\n            }\n\n        logger.info(f\"Calculated forces for {len(member_forces)} members\")\n        return member_forces\n\n    @staticmethod\n    def get_diagram_data(member_forces: Dict[str, Dict],\n                        model: StructuralModel,\n                        num_points: int = 21) -> Dict[str, Dict]:\n        \"\"\"\n        Generate diagram data (SFD, BMD, AFD) for each member.\n\n        Interpolates forces along member length for visualization.\n\n        Parameters\n        ----------\n        member_forces : Dict\n            Forces at element ends from calculate_member_forces()\n        model : StructuralModel\n            Structural model\n        num_points : int\n            Number of points along member for interpolation\n\n        Returns\n        -------\n        diagram_data : Dict[str, Dict]\n            Dictionary with x_values, shear_y, shear_z, moment_y, moment_z, axial\n        \"\"\"\n        diagram_data = {}\n\n        for elem_id, forces in member_forces.items():\n            element = model.elements[elem_id]\n            L = forces['length']\n\n            # X values along member (0 to L)\n            x_vals = np.linspace(0, L, num_points)\n\n            # For linear elements, forces vary linearly between nodes\n            # This is simplified; full solution requires integration of load functions\n\n            # Axial force (constant along length)\n            axial_vals = np.ones(num_points) * forces['axial_i']\n\n            # Shear forces (constant for no distributed load)\n            shear_y_vals = np.ones(num_points) * forces['shear_y_i']\n            shear_z_vals = np.ones(num_points) * forces['shear_z_i']\n\n            # Moments (linear interpolation between node values)\n            moment_y_vals = np.linspace(forces['moment_y_i'],\n                                       forces['moment_y_j'],\n                                       num_points)\n            moment_z_vals = np.linspace(forces['moment_z_i'],\n                                       forces['moment_z_j'],\n                                       num_points)\n\n            diagram_data[elem_id] = {\n                'x_values': x_vals.tolist(),\n                'axial': axial_vals.tolist(),\n                'shear_y': shear_y_vals.tolist(),\n                'shear_z': shear_z_vals.tolist(),\n                'moment_y': moment_y_vals.tolist(),\n                'moment_z': moment_z_vals.tolist(),\n            }\n\n        return diagram_data\n\n    @staticmethod\n    def max_forces(member_forces: Dict[str, Dict]) -> Dict[str, Tuple[str, float64]]:\n        \"\"\"\n        Find maximum absolute forces in structure.\n\n        Useful for design checks and result summaries.\n\n        Parameters\n        ----------\n        member_forces : Dict\n            Member forces from calculate_member_forces()\n\n        Returns\n        -------\n        max_dict : Dict[str, Tuple[str, float]]\n            Maximum force for each force type:\n            {'max_axial': ('M1', 120.5), 'max_shear_y': ('M2', 50.3), ...}\n        \"\"\"\n        max_dict = {}\n\n        force_types = [\n            'axial_i', 'shear_y_i', 'shear_z_i',\n            'moment_x_i', 'moment_y_i', 'moment_z_i'\n        ]\n\n        for ftype in force_types:\n            max_member = None\n            max_value = 0\n\n            for melem_id, forces in member_forces.items():\n                # Get both start and end values\n                for suffix in ['_i', '_j']:\n                    if suffix in ftype:\n                        val = abs(forces[ftype])\n                        if val > max_value:\n                            max_value = val\n                            max_member = melem_id\n\n            if max_member:\n                max_dict[f'max_{ftype}'] = (max_member, float64(max_value))\n\n        return max_dict\n\n\nif __name__ == \"__main__\":\n    logging.basicConfig(level=logging.INFO)\n\n    print(\"Member force calculator module loaded\")\n
"""\napi_routes.py - FastAPI endpoints for structural analysis\n\nExposes DSM solver via REST API:\nPOST /api/v1/analysis/structural - Submit & solve structure\nGET /api/v1/analysis/structural/{job_id} - Retrieve results\n\"\"\"\n\nfrom fastapi import APIRouter, HTTPException, BackgroundTasks\nfrom pydantic import BaseModel, Field\nfrom typing import Dict, List, Optional\nfrom datetime import datetime\nimport uuid\nimport logging\n\nimport numpy as np\nfrom numpy import float64, ndarray\n\nfrom dsm_3d_frame import StructuralModel, Node3D, Element3D, DirectStiffnessMethod3D\nfrom json_handler import JSONHandler, OutputFormatter\nfrom member_forces import MemberForceCalculator\nfrom load_combinations import LoadCombinator, LoadCase\nfrom code_compliance import CodeCompliance\n\nlogger = logging.getLogger(__name__)\nrouter = APIRouter(prefix=\"/api/v1/analysis\", tags=[\"structural\"])\n\n# In-memory job storage (replace with database in production)\nJOB_STORAGE: Dict[str, Dict] = {}\n\n\n# ================================================================\n# Request/Response Models\n# ================================================================\n\nclass NodeInput(BaseModel):\n    \"\"\"Node definition in JSON payload.\"\"\"\n    id: str\n    x: float = Field(description=\"X coordinate (m)\")\n    y: float = Field(description=\"Y coordinate (m)\")\n    z: float = Field(description=\"Z coordinate (m)\")\n\n\nclass RestraintInput(BaseModel):\n    \"\"\"Boundary condition for a node.\"\"\"\n    node_id: str\n    ux: bool = False  # Restrained in X?\n    uy: bool = False\n    uz: bool = False\n    rx: bool = False  # Rotation restrained?\n    ry: bool = False\n    rz: bool = False\n\n\nclass ElementInput(BaseModel):\n    \"\"\"Member/element definition.\"\"\"\n    id: str\n    start_node_id: str = Field(alias=\"startNodeId\")\n    end_node_id: str = Field(alias=\"endNodeId\")\n    E: float = 30000  # Young's modulus (MPa), default concrete\n    G: float = 12000  # Shear modulus (MPa)\n    A: float = 0.25   # Cross-sectional area (m²)\n    Iy: float = 0.002 # Second moment about Y (m⁴)\n    Iz: float = 0.002 # Second moment about Z (m⁴)\n    J: float = 0.0015 # Polar moment (m⁴)\n\n    class Config:\n        populate_by_name = True  # Allow both 'start_node_id' and 'startNodeId'\n\n\nclass LoadInput(BaseModel):\n    \"\"\"Nodal load.\"\"\"\n    node_id: str\n    Px: float = 0  # Force X (kN)\n    Py: float = 0\n    Pz: float = 0\n    Mx: float = 0  # Moment (kN·m)\n    My: float = 0\n    Mz: float = 0\n\n\nclass AnalysisRequest(BaseModel):\n    \"\"\"Complete structural analysis request.\"\"\"\n    job_name: str = \"Unnamed Analysis\"\n    code: str = \"is456\"  # Code type: is456, is800\n    material_grade: str = \"M30\"  # M20, M25, M30, etc.\n\n    nodes: List[NodeInput]\n    elements: List[ElementInput]\n    restraints: List[RestraintInput]\n    loads: List[LoadInput]\n\n    generate_combinations: bool = True\n    check_compliance: bool = True\n\n\nclass DisplacementResult(BaseModel):\n    \"\"\"Node displacement result.\"\"\"\n    node_id: str\n    ux: float  # Displacement X (mm)\n    uy: float\n    uz: float\n    rx: float  # Rotation (rad)\n    ry: float\n    rz: float\n\n\nclass ReactionResult(BaseModel):\n    \"\"\"Support reaction.\"\"\"\n    node_id: str\n    Rx: float = 0  # Reaction force (kN)\n    Ry: float = 0\n    Rz: float = 0\n    Mx: float = 0  # Reaction moment (kN·m)\n    My: float = 0\n    Mz: float = 0\n\n\nclass MemberForceResult(BaseModel):\n    \"\"\"Internal forces in a member.\"\"\"\n    member_id: str\n    axial_i: float      # At start node (kN)\n    shear_y_i: float\n    shear_z_i: float\n    moment_x_i: float   # Torsion (kN·m)\n    moment_y_i: float\n    moment_z_i: float\n    axial_j: float      # At end node\n    shear_y_j: float\n    shear_z_j: float\n    moment_x_j: float\n    moment_y_j: float\n    moment_z_j: float\n    length: float\n\n\nclass AnalysisResponse(BaseModel):\n    \"\"\"Complete analysis results.\"\"\"\n    job_id: str\n    job_name: str\n    status: str  # 'success', 'failed', 'running'\n    timestamp: str\n    execution_time_seconds: float\n\n    # Model info\n    n_nodes: int\n    n_elements: int\n    n_dofs: int\n\n    # Results\n    displacements: List[DisplacementResult] = []\n    reactions: List[ReactionResult] = []\n    member_forces: List[MemberForceResult] = []\n\n    # Optional\n    combinations_analyzed: List[str] = []\n    compliance_checks: Optional[Dict] = None\n    max_displacement: float = 0\n    max_reaction: float = 0\n    max_member_force: float = 0\n\n    error_message: Optional[str] = None\n\n\n# ================================================================\n# Endpoints\n# ================================================================\n\n@router.post(\"/structural\", response_model=AnalysisResponse)\ndef analyze_structure(request: AnalysisRequest) -> AnalysisResponse:\n    \"\"\"\n    Submit a structure for analysis.\n\n    Accepts nodes, elements, restraints, and loads.\n    Solves Direct Stiffness Method 3D frame problem.\n    Returns displacements, reactions, and member forces.\n\n    Example JSON body:\n    ```json\n    {\n      \"job_name\": \"Simple Cantilever\",\n      \"code\": \"is456\",\n      \"material_grade\": \"M30\",\n      \"nodes\": [\n        {\"id\": \"N1\", \"x\": 0, \"y\": 0, \"z\": 0},\n        {\"id\": \"N2\", \"x\": 5, \"y\": 0, \"z\": 0}\n      ],\n      \"elements\": [\n        {\n          \"id\": \"M1\",\n          \"startNodeId\": \"N1\",\n          \"endNodeId\": \"N2\",\n          \"E\": 30000,\n          \"A\": 0.25,\n          \"Iy\": 0.002\n        }\n      ],\n      \"restraints\": [\n        {\"node_id\": \"N1\", \"ux\": true, \"uy\": true, \"uz\": true,\n         \"rx\": true, \"ry\": true, \"rz\": true}\n      ],\n      \"loads\": [\n        {\"node_id\": \"N2\", \"Py\": -50}\n      ],\n      \"generate_combinations\": true,\n      \"check_compliance\": true\n    }\n    ```\n    \"\"\"\n    job_id = str(uuid.uuid4())\n    start_time = datetime.now()\n\n    try:\n        # ================================================================\n        # STEP 1: Parse JSON to model\n        # ================================================================\n        logger.info(f\"[{job_id}] Parsing model: {request.job_name}\")\n\n        # Build nodes dictionary\n        nodes = {}\n        for node_input in request.nodes:\n            restraints = {}\n            for restraint in request.restraints:\n                if restraint.node_id == node_input.id:\n                    restraints = {\n                        'ux': restraint.ux,\n                        'uy': restraint.uy,\n                        'uz': restraint.uz,\n                        'rx': restraint.rx,\n                        'ry': restraint.ry,\n                        'rz': restraint.rz,\n                    }\n                    break\n\n            nodes[node_input.id] = Node3D(\n                id=node_input.id,\n                x=float64(node_input.x),\n                y=float64(node_input.y),\n                z=float64(node_input.z),\n                restraints=restraints\n            )\n\n        # Build elements dictionary\n        elements = {}\n        for elem_input in request.elements:\n            elements[elem_input.id] = Element3D(\n                id=elem_input.id,\n                node_i=elem_input.start_node_id,\n                node_j=elem_input.end_node_id,\n                E=float64(elem_input.E),\n                G=float64(elem_input.G),\n                A=float64(elem_input.A),\n                Iy=float64(elem_input.Iy),\n                Iz=float64(elem_input.Iz),\n                J=float64(elem_input.J),\n            )\n\n        # Create model\n        model = StructuralModel(nodes=nodes, elements=elements)\n        logger.info(f\"[{job_id}] Model created: {model.n_nodes} nodes, \" \n                   f\"{model.n_elements} elements, {model.n_dofs} DOFs\")\n\n        # ================================================================\n        # STEP 2: Build load vector\n        # ================================================================\n        F = np.zeros(model.n_dofs, dtype=float64)\n        node_list = sorted(model.nodes.keys())\n        node_to_index = {nid: idx for idx, nid in enumerate(node_list)}\n\n        for load in request.loads:\n            if load.node_id not in model.nodes:\n                logger.warning(f\"Load on unknown node: {load.node_id}\")\n                continue\n\n            node_idx = node_to_index[load.node_id]\n            dof_base = node_idx * 6\n\n            F[dof_base + 0] += float64(load.Px)\n            F[dof_base + 1] += float64(load.Py)\n            F[dof_base + 2] += float64(load.Pz)\n            F[dof_base + 3] += float64(load.Mx)\n            F[dof_base + 4] += float64(load.My)\n            F[dof_base + 5] += float64(load.Mz)\n\n        logger.info(f\"[{job_id}] Load vector assembled: {np.count_nonzero(F)} non-zero entries\")\n\n        # ================================================================\n        # STEP 3: Solve DSM\n        # ================================================================\n        logger.info(f\"[{job_id}] Solving DSM...\")\n        dsm = DirectStiffnessMethod3D()\n        dsm.build_model(model)\n        dsm.assemble_system(model, F)\n        U = dsm.solve()  # Displacements\n        dsm.back_calculate_reactions()\n\n        logger.info(f\"[{job_id}] Solve complete: max displacement = {np.max(np.abs(U)):.6e}\")\n\n        # ================================================================\n        # STEP 4: Calculate member forces\n        # ================================================================\n        member_forces = MemberForceCalculator.calculate_member_forces(model, U)\n        logger.info(f\"[{job_id}] Member forces calculated\")\n\n        # ================================================================\n        # STEP 5: Format results\n        # ================================================================\n        # Displacements\n        displacement_results = []\n        for i, node_id in enumerate(node_list):\n            dofs = i * 6\n            displacement_results.append(\n                DisplacementResult(\n                    node_id=node_id,\n                    ux=float(U[dofs + 0] * 1000),    # Convert m to mm\n                    uy=float(U[dofs + 1] * 1000),\n                    uz=float(U[dofs + 2] * 1000),\n                    rx=float(U[dofs + 3]),            # Radians\n                    ry=float(U[dofs + 4]),\n                    rz=float(U[dofs + 5])\n                )\n            )\n\n        # Reactions\n        reaction_results = []\n        for i, node_id in enumerate(node_list):\n            node = model.nodes[node_id]\n            # Only show reactions at restrained nodes\n            has_restraint = any(node.restraints.values())\n            if has_restraint:\n                dofs = i * 6\n                reaction_results.append(\n                    ReactionResult(\n                        node_id=node_id,\n                        Rx=float(dsm.R[dofs + 0]) if dofs + 0 < len(dsm.R) else 0,\n                        Ry=float(dsm.R[dofs + 1]) if dofs + 1 < len(dsm.R) else 0,\n                        Rz=float(dsm.R[dofs + 2]) if dofs + 2 < len(dsm.R) else 0,\n                        Mx=float(dsm.R[dofs + 3]) if dofs + 3 < len(dsm.R) else 0,\n                        My=float(dsm.R[dofs + 4]) if dofs + 4 < len(dsm.R) else 0,\n                        Mz=float(dsm.R[dofs + 5]) if dofs + 5 < len(dsm.R) else 0,\n                    )\n                )\n\n        # Member forces\n        member_force_results = []\n        for member_id, forces in member_forces.items():\n            member_force_results.append(\n                MemberForceResult(\n                    member_id=member_id,\n                    axial_i=float(forces['axial_i']),\n                    shear_y_i=float(forces['shear_y_i']),\n                    shear_z_i=float(forces['shear_z_i']),\n                    moment_x_i=float(forces['moment_x_i']),\n                    moment_y_i=float(forces['moment_y_i']),\n                    moment_z_i=float(forces['moment_z_i']),\n                    axial_j=float(forces['axial_j']),\n                    shear_y_j=float(forces['shear_y_j']),\n                    shear_z_j=float(forces['shear_z_j']),\n                    moment_x_j=float(forces['moment_x_j']),\n                    moment_y_j=float(forces['moment_y_j']),\n                    moment_z_j=float(forces['moment_z_j']),\n                    length=float(forces['length'])\n                )\n            )\n\n        # ================================================================\n        # STEP 6: Code compliance checks (optional)\n        # ================================================================\n        compliance_checks = None\n        if request.check_compliance:\n            max_stress = np.max(np.abs(U))  # Simplified\n            max_deflection = np.max(np.abs(U)) * 1000  # mm\n            compliance_checks = CodeCompliance.run_checks(\n                {\n                    'max_stress': max_stress,\n                    'max_deflection': max_deflection,\n                    'span': 5000,  # Default\n                },\n                code_type=request.code,\n                material_grade=request.material_grade\n            )\n            logger.info(f\"[{job_id}] Compliance checks: \"\n                       f\"{len(compliance_checks.get('failed', []))} failed\")\n\n        # ================================================================\n        # STEP 7: Assemble response\n        # ================================================================\n        execution_time = (datetime.now() - start_time).total_seconds()\n\n        response = AnalysisResponse(\n            job_id=job_id,\n            job_name=request.job_name,\n            status='success',\n            timestamp=start_time.isoformat(),\n            execution_time_seconds=execution_time,\n            n_nodes=model.n_nodes,\n            n_elements=model.n_elements,\n            n_dofs=model.n_dofs,\n            displacements=displacement_results,\n            reactions=reaction_results,\n            member_forces=member_force_results,\n            compliance_checks=compliance_checks,\n            max_displacement=max(abs(d.uy) for d in displacement_results) if displacement_results else 0,\n            max_reaction=max(abs(r.Ry) for r in reaction_results) if reaction_results else 0,\n            max_member_force=max(\n                max(abs(m.shear_y_i), abs(m.moment_y_i)) \n                for m in member_force_results\n            ) if member_force_results else 0,\n        )\n\n        # Store job\n        JOB_STORAGE[job_id] = response.model_dump()\n\n        logger.info(f\"[{job_id}] ✓ Analysis complete in {execution_time:.2f}s\")\n        return response\n\n    except Exception as e:\n        logger.error(f\"[{job_id}] ✗ Analysis failed: {str(e)}\", exc_info=True)\n\n        execution_time = (datetime.now() - start_time).total_seconds()\n        error_response = AnalysisResponse(\n            job_id=job_id,\n            job_name=request.job_name,\n            status='failed',\n            timestamp=start_time.isoformat(),\n            execution_time_seconds=execution_time,\n            n_nodes=0,\n            n_elements=0,\n            n_dofs=0,\n            error_message=str(e)\n        )\n        return error_response\n\n\n@router.get(\"/structural/{job_id}\", response_model=AnalysisResponse)\ndef get_analysis_result(job_id: str) -> AnalysisResponse:\n    \"\"\"\n    Retrieve previously computed analysis results.\n    \"\"\"\n    if job_id not in JOB_STORAGE:\n        raise HTTPException(status_code=404, detail=f\"Job {job_id} not found\")\n\n    return AnalysisResponse(**JOB_STORAGE[job_id])\n\n\n@router.get(\"/structural-list\")\ndef list_jobs() -> Dict:\n    \"\"\"\n    List all completed analyses.\n    \"\"\"\n    return {\n        'count': len(JOB_STORAGE),\n        'jobs': [\n            {\n                'job_id': jid,\n                'name': job['job_name'],\n                'status': job['status'],\n                'timestamp': job['timestamp'],\n                'n_nodes': job['n_nodes'],\n                'n_elements': job['n_elements'],\n            }\n            for jid, job in JOB_STORAGE.items()\n        ]\n    }\n\n\nif __name__ == \"__main__\":\n    # For testing\n    logger.basicConfig(level=logging.INFO)\n    print(\"API routes module loaded\")\n
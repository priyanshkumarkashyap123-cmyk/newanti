<?xml version="1.0" encoding="utf-8"?>
<configuration>
  <system.webServer>
    <!-- 
      NOTE: IIS Node handler is disabled. Startup is driven by Azure App Service startup command.
      See .github/workflows/azure-deploy.yml for the canonical startup configuration.
      Path references below are DEPRECATED and should not be used.
      DO NOT EDIT THIS FILE — use Azure Portal or deployment workflow to set startup commands.
    -->
    <webSocket enabled="false" />
    <!-- iisnode configuration is disabled — Node.js startup via Azure App Service command line only -->
    <!--
    <handlers>
      <add name="iisnode" path="dist/index.cjs" verb="*" modules="iisnode" resourceType="File" />
    </handlers>
    <rewrite>
      <rules>
        <rule name="BodylessRule" patternSyntax="ECMAScript">
          <conditions logicalGrouping="MatchAll">
            <add input="{REQUEST_METHOD}" pattern="^(GET|HEAD|DELETE|OPTIONS)$"/>
          </conditions>
          <action type="Rewrite" url="dist/index.cjs"/>
        </rule>
        <rule name="RestRule" patternSyntax="ECMAScript" stopProcessing="true">
          <conditions logicalGrouping="MatchAll">
            <add input="{REQUEST_FILENAME}" matchType="IsFile" negate="true"/>
            <add input="{REQUEST_FILENAME}" matchType="IsDirectory" negate="true"/>
          </conditions>
          <action type="Rewrite" url="dist/index.cjs"/>
        </rule>
      </rules>
    </rewrite>
    <iisnode 
      node_env="production" 
      nodeProcessCommandLine="node dist/index.cjs"
      loggingEnabled="true"
      logDirectory="iisnode"
      interceptor="iisnode-interceptor.js"
      />
    -->
  </system.webServer>
</configuration>

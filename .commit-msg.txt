fix: launch readiness audit - security hardening & CI/CD fixes

SECURITY (CRITICAL):
- Fix Python auth bypass: INTERNAL_SERVICE_SECRET now rejects empty values
- Fix Python JWT bypass: always reject invalid tokens even without JWT_SECRET
- Add requireAuth() to all Node.js compute routes (analyze/design/advanced/interop/templates/jobs)
- Secure consent route: require Clerk auth, use authenticated userId

PRODUCTION HARDENING:
- Add global exception handlers in Python API to strip error details in production
- Strip console.log/debugger from production frontend builds (esbuild.drop)
- Sanitize 500 errors with generic message in production mode

CI/CD FIXES:
- Replace deprecated actions-rs/toolchain@v1 with dtolnay/rust-toolchain@stable (3 workflows)
- Replace deprecated azure/docker-login@v1 with docker/login-action@v3 (3 workflows)
- Bump actions/checkout@v3 to v4, docker/build-push-action@v4 to v5
- Fix release.yml: add all VITE_* env vars, fix SWA deploy with skip_app_build
- Fix pr.yml: add VITE_CLERK_PUBLISHABLE_KEY and API URLs to build env
- Fix self-hosted SWA: remove broken if condition, NEXT_PUBLIC -> VITE, add Clerk key, fix output_location
